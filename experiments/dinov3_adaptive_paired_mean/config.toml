plant_reference_config_path = "experiments/dinov3_plant_damage_mil/config.toml"
source_manifest = "outputs/dataset_manifests/scored_manifest.csv"
reference_ood_dir = "outputs/dinov3_plant_damage_mil_gold_only/ood_weak_probe_100"
reference_validation_predictions = "outputs/dinov3_plant_damage_mil_gold_only/predictions.csv"
run_dir = "outputs/dinov3_adaptive_paired_mean"
seed = 42
epochs = 60
batch_size = 24
num_workers = 4
learning_rate = 0.0003
weight_decay = 0.001
residual_penalty = 0.01
early_stopping_patience = 12
gradient_clip_norm = 1.0
